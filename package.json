{
  "name": "ordering-dashboard",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"cd server && npm run start:dev\" \"cd client && npm run dev\" ",
    "install:client": "cd client && npm install -D",
    "install:server": "cd server && npm install -D",
    "install:all": "npm i && npm run install:client && npm run install:server"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
